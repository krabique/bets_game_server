<%= simple_form_for(@account) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :amount %>
    <%= f.input :amount_currency, collection: all_currencies %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
